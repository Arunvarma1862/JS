//boolean

let isActive=true;
let hasPermission=false

// Falsy values: false, 0, "" (empty string), null, undefined, and NaN.
// Truthy values: Everything else (e.g., non-empty strings, non-zero numbers, arrays, objects).

// Comparsion operator == vs ===

let a="669"
let b=669;

console.log( a==b)    //  if a and b values are same,true- value only checked
console.log(a === b)  // false - value and data type also checked

// != vs !==
console.log(a!==b);   // true   value and data type checked
console.log(a !=b)    // false  value only checked

